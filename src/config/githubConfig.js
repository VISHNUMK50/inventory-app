// src/config/githubConfig.js
const githubConfig = {
    token: process.env.NEXT_PUBLIC_DATABASE_PAT,
    repo: "inventory-app",
    owner: "VISHNUMK50",
    branch: "master",
    path: "database"
  };
  
  export default githubConfig;